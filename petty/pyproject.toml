[project]
name = "petty"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
  "cryptography>=48.0.0",
  "orjson>=3.11.7",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["petty"]
